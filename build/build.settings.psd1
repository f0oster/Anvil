# Build settings for Anvil
#
# Edit these values to customise the build pipeline.  The build script
# (module.build.ps1) reads this file at startup via Import-PowerShellDataFile.
@{
    ModuleName        = 'Anvil'
    CoverageThreshold = 80
}
